.build*/
.swiftpm/
*.app
*.dmg
dmg-staging/
*.zip
.DS_Store
xcuserdata/
DerivedData/
*.cert
*.p12
*.key
*.csr

# Local environment secrets and keys
.env
.env.*
!.env.example
*.secret

# Generated at build time by Scripts/inject-secrets.sh from .env.secrets or CI
# secrets. Never tracked — the XOR masking is trivially reversible, so a
# committed copy publishes the live credentials.
Sources/Hisingen/Services/API/GeneratedVolvoSecrets.swift

Hisingen.dmg.sha256
# Reverse-engineering deep-dives — internal only, never upload
docs/api/polestar-backend-map.md
docs/api/polestar-probe-transcripts.md
docs/api/volvo-backend-map.md
docs/api/ota-investigation.md
